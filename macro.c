#include<stdio.h>
#define PI 3.14
#define MUL(a,b)
#define MAX(x,y) if(x>y) \
                 printf("%d is greater",x); \
                 else \
                 printf("%d is greater",y); \

int main()
{
    int r=10;
    printf("area of circle = %f\n",(PI*r*r));
    printf("multiply = %d\n",(2*4));
    return 0;
}