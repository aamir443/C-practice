// reverse the string using strrev function 
#include<stdio.h>
#include<string.h>
int main()
    {
       string(char[])
       printf("enter a string : \n");
       scanf("%s",&str);

       printf("reverse of string : %s",strrev(str)); //strrev() is a function which is reverse the string
       return 0;
    }
